@model IEnumerable<MidtermReview1.Models.Position>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pos_code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pos_desc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pos_code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pos_desc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pos_code  }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.Pos_code })
        </td>
    </tr>
}

</table>
